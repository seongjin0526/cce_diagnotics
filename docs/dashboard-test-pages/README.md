# Dashboard Test Pages

Static Flask test-client snapshots for quickly reviewing the dashboard without
binding a local server.

Open `index.html` first, then use the links to inspect the exported pages.

Regenerate with:

```bash
python3 tools/export_dashboard_test_pages.py
```
