worker_processes  1;

events {
    worker_connections  1024;
}

http {
    server {
        listen 80;
        server_name _;

        location / {
            default_type text/plain;
            return 200 "nginx lab\n";
        }
    }
}
